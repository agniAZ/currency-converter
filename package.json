{
  "name": "adyen-currency-converter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.1"
  },
  "scripts": {
    "clean": "rimraf dist/*",
    "serve": "browser-sync start --server \"dist\" --files \"dist\"",
    "lite": "lite-server --port 8000",
    "start": "npm run lite"
  },
  "devDependencies": {
    "lite-server": "^1.3.1"
  }
}
